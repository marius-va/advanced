NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-12-06

# Draft/Preview Mode (get token from Sanity dashboard: API > Tokens > Create with "Viewer" permissions)
SANITY_API_READ_TOKEN=

# Preview Secret (generate with: openssl rand -base64 32)
SANITY_PREVIEW_SECRET=

# Webhook Revalidation Secret (generate with: openssl rand -base64 32)
SANITY_REVALIDATE_SECRET=
